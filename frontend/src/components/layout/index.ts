/**
 * Layout Components
 * Export all layout components for easy importing
 */

export { default as AppLayout } from './AppLayout.vue'
export { default as AppSidebar } from './AppSidebar.vue'
export { default as AppHeader } from './AppHeader.vue'
// Fork-local kissopen split-screen auth shell (replaces the upstream single-card
// AuthLayout). The original remains at ./AuthLayout.vue for easy fork sync.
export { default as AuthLayout } from './KissopenAuthLayout.vue'
